# Rust build output (shared by the whole workspace)
/target/

# Trunk's web/wasm build output
/gui/dist/

# macOS
.DS_Store

# Xcode / iOS — user-specific and generated files
ios/**/xcuserdata/
ios/**/*.xcuserstate
ios/**/DerivedData/
ios/**/build/
ios/**/*.xcworkspace/xcshareddata/
*.ipa
*.dSYM
*.dSYM.zip

# Rust build artifacts that `cargo make ios` copies in — regenerated every build
ios/FifteenPuzzle/lib/
ios/FifteenPuzzle/include/

# Generated app icons - `cargo make icons` renders these from res/app_icon.svg,
# which is the only icon file that should actually be tracked in git.
/android_res/
/res/app_icon_1024@2x.png

# Editors
.vscode/
.idea/
*.swp
